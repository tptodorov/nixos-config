# Age encryption keys (never commit these!)
**/.config/sops/age/keys.txt
**/age/keys.txt

# Legacy plain text secrets (if any exist)
home/*/secrets/local.nix
secrets/*.env
secrets/*.key
secrets/*.pem

# Build results
result/
result-*

# Stray lock-file backups (never a real artifact worth versioning)
*.lock.backup

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db
result
*.log
.cache/nix
.codex
.tmp-cache/nix
.tmp-home
